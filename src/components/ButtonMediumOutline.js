import React from "react";

const ButtonMediumOutline = ({ name }) => {
  return (
    <button className="border-2 border-primary text-center p-2 w-full rounded-md text-primary">
      {name}
    </button>
  );
};

export default ButtonMediumOutline;
